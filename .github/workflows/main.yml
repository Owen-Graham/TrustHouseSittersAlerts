# .github/workflows/scrape.yml
name: Scrape TrustedHousesitters

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    env:
      # these will be picked up by scraper.py
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      EMAIL:              ${{ secrets.EMAIL }}
      PASSWORD:           ${{ secrets.PASSWORD }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          persist-credentials: true
          fetch-depth: 0

      - name: Set up Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install playwright pandas requests python-dotenv
          playwright install

      - name: Run scraper
        # GitHub Actions is headless by default; scraper.py should detect GITHUB_ACTIONS and run headless
        run: |
          python scraper.py

      - name: Commit updated CSV/JSON
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add sits.csv sits.json
          if ! git diff --cached --quiet; then
            git commit -m 'chore: update sits.csv + sits.json [skip ci]'
            git push
          else
            echo "No changes to commit"
          fi
